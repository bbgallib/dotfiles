# Manual install apps

These are not added to Homebrew (or are likely to be installed via other means).

- Xcode (App Store)
- RunCat (App Store)
- Google apps other than Drive (Docs/Sheets/Slides): install as web apps or use browser
